{
  "name": "todo_application",
  "version": "5.0.0",
  "description": "using nignx, Vite, and React.",
  "private": true,
  "type": "module",
  "workspaces": [
    "api"
  ],
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "start:dev": "docker compose -f compose.dev.yaml watch",
    "start": "docker compose watch",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:ps": "docker compose ps",
    "install:all": "npm install"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  }
}
